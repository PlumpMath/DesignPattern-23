*定义：
将对象以树形结构组织起来,以达成“部分－整体” 的层次结构，使得客户端对单个对象和组合对象的使用具有一致性.

*角色：
1)component 组件
2)compsite 复合组件（本身也是组件，而且包含一个或多个其他组件）
3)leaf 简单组件

*
组合模式的关键在于：将对象组织成树形结构，表达整体与部分的层次


*
引入Composite模式后，客户端程序不再依赖于复合元素的内部实现了