*定义：
为其他对象提供一种代理以控制对这个对象的访问.

*角色：
1)Subject 抽象被代理类
2)RealSubject 具体的被代理类
3)Proxy 代理类

*
代理模式的关键在于：间接访问对象


*
1.使用代理模式原因：
  a.授权机制 不同级别的用户对同一对象拥有不同的访问权利
  b.某个客户端不能直接操作到某个对象,但又必须和那个对象有所互动
2.代理对象，往往是做一些规则限制来访问xx对象

*
应用场合：
大对象访问、同步对象的拷贝、控制访问权限、分布式远程对象的访问


